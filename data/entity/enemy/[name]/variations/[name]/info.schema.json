{
	"$schema": "https://json-schema.org/draft-07/schema#",
	"type": "object",
	"description": "Sobrescrita opcional das configurações do inimigo raiz.",
	"properties": {
		"name": {
			"type": "string",
			"description": "Nome da variação do inimigo."
		},
		"description": {
			"type": "string",
			"description": "Descrição da variação do inimigo."
		},
		"metadata": {
			"type": "object",
			"properties": {
				"tile-size": {
					"type": "integer",
					"description": "Sobrescreve o tamanho de cada tile no spritesheet."
				},
				"animations": {
					"type": "object",
					"patternProperties": {
						".*": {
							"type": "object",
							"properties": {
								"frames": {
									"type": "array",
									"items": { "type": "integer" },
									"description": "Sobrescreve os índices dos frames para esta animação."
								},
								"speed": {
									"type": "number",
									"description": "Sobrescreve a velocidade da animação em frames por segundo."
								},
								"trigger": {
									"type": "string",
									"description": "Sobrescreve o evento que ativa a animação."
								}
							}
						}
					},
					"description": "Sobrescreve as animações e seus triggers."
				}
			}
		},
		"default": {
			"type": "object",
			"properties": {
				"health": {
					"type": "number",
					"description": "Sobrescreve a vida inicial do inimigo."
				},
				"damage": {
					"type": "number",
					"description": "Sobrescreve o dano que o inimigo causa."
				},
				"movement-pattern": {
					"type": "string",
					"enum": [
						"patrol",
						"stationary",
						"chase",
						"wander",
						"flee",
						"orbit",
						"strafe",
						"follow-leader",
						"jump",
						"zigzag",
						"teleport"
					],
					"description": "Sobrescreve o padrão de movimento do inimigo."
				},
				"attack-type": {
					"type": "string",
					"enum": [
						"melee",
						"ranged",
						"area",
						"status-effect",
						"dash",
						"charge",
						"trap",
						"drain",
						"summon",
						"reflect",
						"self-destruct",
						"multi"
					],
					"description": "Sobrescreve o tipo de ataque do inimigo."
				},
				"speed": {
					"type": "number",
					"description": "Sobrescreve a velocidade de movimento do inimigo."
				}
			}
		}
	},
    "additionalProperties": false
}
